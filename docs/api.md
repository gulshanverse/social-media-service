# API

The API is hosted by NestJS on port 4000 in local development. `GET /health` is the foundation health endpoint. Future phases will add versioned confession, authentication, moderation, report, theme, and analytics endpoints with DTO validation and consistent response envelopes.
